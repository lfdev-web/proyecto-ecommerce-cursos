# Django y REST
Django>=5.0,<6.0
djangorestframework>=3.15,<4.0
django-cors-headers>=4.3,<5.0

# Base de datos
psycopg2-binary>=2.9,<3.0

# Autenticación JWT
djangorestframework-simplejwt>=5.3,<6.0

# Tareas asíncronas y programación
celery>=5.3,<6.0
redis>=5.0,<6.0
django-celery-beat>=2.5,<3.0

# Generación de PDFs para facturas y certificados
WeasyPrint>=62.0,<63.0
# Pin necesario: pydyf>=0.11 rompe la API interna que usa WeasyPrint 62.x
# (AttributeError: 'super' object has no attribute 'transform' al generar PDFs)
pydyf==0.10.0

# Machine Learning para Recomendaciones CF (SVD)
scikit-learn>=1.5,<2.0
numpy>=1.26,<2.0

# Procesamiento de imágenes (requerido por django ImageField/WeasyPrint)
Pillow>=10.2,<11.0

# Documentación de la API (Swagger)
drf-spectacular>=0.27,<1.0

# Servidor WSGI de producción. `manage.py runserver` es solo para desarrollo:
# es de un solo hilo y la propia documentación de Django prohíbe usarlo en
# producción. gunicorn atiende varios trabajadores en paralelo.
gunicorn>=22.0,<24.0
